name: Publish Module
on:
  workflow_dispatch:
  push:
    tags:
    - '*'

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  publish_module:
    name: "Publish Module"
    runs-on: ubuntu-latest
    steps:
    # Checkout repository
    - uses: actions/checkout@v3
      with:
        path: libraries/bzlmodrio-revlib

    - name: Create Archive
      run: git archive HEAD --format=tar.gz --output="bzlmodrio-revlib-${GITHUB_REF_NAME}.tar.gz"
      working-directory: libraries/bzlmodrio-revlib

    - name: Setup archive name
      run: echo "ARCHIVE_NAME=libraries/bzlmodrio-revlib/bzlmodrio-revlib-${GITHUB_REF_NAME}.tar.gz" >> "$GITHUB_ENV"

    - name: Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: "${{ env.ARCHIVE_NAME }}"


    # Checkout bzlmodrio-allwpilib
    - uses: actions/checkout@v3
      with:
        repository: 'bzlmodRio/bzlmodrio-allwpilib.git'
        fetch-depth: 0
        path: libraries/bzlmodrio-revlib/../../libraries/bzlmodRio-allwpilib
        ref: refactor_dev

    # Checkout bzlmodrio-opencv
    - uses: actions/checkout@v3
      with:
        repository: 'bzlmodRio/bzlmodrio-opencv.git'
        fetch-depth: 0
        path: libraries/bzlmodrio-revlib/../../libraries/bzlmodRio-opencv
        ref: refactor_dev

    # Checkout bzlmodrio-ni
    - uses: actions/checkout@v3
      with:
        repository: 'bzlmodRio/bzlmodrio-ni.git'
        fetch-depth: 0
        path: libraries/bzlmodrio-revlib/../../libraries/bzlmodRio-ni
        ref: refactor_dev




    # Checkout gentool
    - uses: actions/checkout@v3
      with:
        repository: 'bzlmodRio/gentool.git'
        fetch-depth: 0
        path: gentool
        ref: refactor_dev

    # Checkout BCR
    - uses: actions/checkout@v3
      with:
        repository: 'bzlmodRio/bazel-central-registry.git'
        fetch-depth: 0
        path: bazel-central-registry
        ref: bzlmodrio

    # Run publishing
    - run: bazel run //:publish
      working-directory: libraries/bzlmodrio-revlib/generate

    # Create pull requests
    - name: Create Gentool Pull Request
      uses: peter-evans/create-pull-request@v4
      env:
        GITHUB_TOKEN: ${{ secrets.AUTO_UPDATE_KEY }}
      with:
        path: gentool
        base: refactor_dev
        token: ${{ secrets.AUTO_UPDATE_KEY }}
        reviewers: pjreiniger
        branch: autoupdate/bzlmodrio-revlib_update
        title: "Auto-update latest bzlmodrio-revlib module release"

    - name: Create BCR Pull Request
      uses: peter-evans/create-pull-request@v4
      env:
        GITHUB_TOKEN: ${{ secrets.AUTO_UPDATE_KEY }}
      with:
        path: bazel-central-registry
        base: bzlmodrio
        token: ${{ secrets.AUTO_UPDATE_KEY }}
        reviewers: pjreiniger
        branch: autoupdate/bzlmodrio-revlib_update
        title: "Auto-update latest bzlmodrio-revlib module release"
